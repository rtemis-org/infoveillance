% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gdelt.R
\name{query_gdelt}
\alias{query_gdelt}
\title{Query GDELT for news articles matching keywords}
\usage{
query_gdelt(
  keywords,
  match = c("all", "any"),
  mode = c("artlist", "timelinevol", "timelinevolraw", "timelinetone", "timelinelang",
    "timelinesourcecountry", "tonechart"),
  timespan = NULL,
  start_datetime = NULL,
  end_datetime = NULL,
  source_lang = NULL,
  source_country = NULL,
  domain = NULL,
  theme = NULL,
  tone = NULL,
  max_records = 250L,
  sort = c("datedesc", "dateasc", "tonedesc", "toneasc", "hybridrel"),
  timeline_smooth = NULL,
  timeout = 60,
  verbosity = 1L
)
}
\arguments{
\item{keywords}{Character: One or more keywords or phrases to search for.}

\item{match}{Character: \code{"all"} requires every keyword; \code{"any"} requires at
least one.}

\item{mode}{Character: What to return. \code{"artlist"} returns matching
articles; \code{"timelinevol"} the share of all monitored coverage matching the
query over time; \code{"timelinevolraw"} the raw article counts together with
the total monitored (\code{norm}); \code{"timelinetone"} average tone over time;
\code{"timelinelang"} and \code{"timelinesourcecountry"} volume over time split by
language or source country; \code{"tonechart"} a histogram of article tone.}

\item{timespan}{Character or NULL: Search window ending now, e.g. \code{"1w"}.
Not used together with \code{start_datetime}/\code{end_datetime}.}

\item{start_datetime}{Date, POSIXct, or character or NULL: Start of the
search window, in UTC. A character value must be \code{"YYYYMMDDHHMMSS"}.}

\item{end_datetime}{Date, POSIXct, or character or NULL: End of the search
window, in UTC. A \code{Date} end covers the whole day.}

\item{source_lang}{Character or NULL: Language(s) of the source article, as
a name (\code{"english"}) or ISO 639 code (\code{"eng"}).}

\item{source_country}{Character or NULL: Country/ies the source outlet is
based in, as a name (\code{"unitedstates"}) or FIPS code (\code{"US"}).}

\item{domain}{Character or NULL: Source domain(s), e.g. \code{"nytimes.com"}.}

\item{theme}{Character or NULL: GDELT Global Knowledge Graph theme(s), e.g.
\code{"TAX_DISEASE_CORONAVIRUS"}.}

\item{tone}{Character or NULL: Tone filter, a comparison such as \code{">5"}
(positive) or \code{"<-5"} (negative).}

\item{max_records}{Integer: Maximum number of articles to return in
\code{"artlist"} mode, at most \code{250L}. Ignored by other modes.}

\item{sort}{Character: Order of articles in \code{"artlist"} mode.}

\item{timeline_smooth}{Integer or NULL: Moving-average window (in time
steps, up to \code{30L}) applied to timeline modes.}

\item{timeout}{Numeric: Seconds to wait for a response.}

\item{verbosity}{Integer: Verbosity level.}
}
\value{
\code{data.table}. In \code{"artlist"} mode, one row per article with columns
\code{url}, \code{url_mobile}, \code{title}, \code{seendate} (POSIXct, UTC), \code{socialimage},
\code{domain}, \code{language}, and \code{sourcecountry}. In timeline modes, one row per
time step with columns \code{series}, \code{date} (POSIXct, UTC), \code{value}, and, for
\code{"timelinevolraw"}, \code{norm}. In \code{"tonechart"} mode, one row per tone bin
with columns \code{bin}, \code{count}, and \code{toparts} (a list of \code{data.frame}s of
sample articles). A query with no matches returns an empty \code{data.table}
with the same columns. The query string sent to GDELT and the full request
URL are attached as attributes \code{"query"} and \code{"url"}.
}
\description{
Search the GDELT Global Knowledge Graph's DOC 2.0 API for online news
coverage matching one or more keywords, and return either the matching
articles or an aggregate timeline of coverage volume, tone, language, or
source country. Coverage begins on 2017-01-01 and is updated every 15
minutes; a query with no explicit window searches the last three months.
}
\details{
Each keyword is one search term; a keyword containing whitespace is
searched as an exact phrase. With \code{match = "all"} every keyword must appear
in an article; with \code{match = "any"} at least one must. GDELT requires each
term to be at least three characters and rejects terms that are too common
to search on. Filters are combined with the keywords: an article must be in
one of \code{source_lang}, from one of \code{source_country}, published on one of
\code{domain}, tagged with one of \code{theme}, and have a tone within \code{tone}.

\code{timespan} and \code{start_datetime}/\code{end_datetime} are two ways to set the
search window; supply one or the other. \code{timespan} is a number followed by
a unit: \code{min} (minutes), \code{h} (hours), \code{d} (days), \code{w} (weeks), or \code{m}
(months), e.g. \code{"24h"} or \code{"2w"}.

GDELT accepts at most one request every five seconds per client.
\code{query_gdelt()} makes one request per call and waits as needed so that
repeated calls in the same session respect that limit; if GDELT still
rejects a request as too frequent, it is retried once after a pause.
}
\examples{
\dontrun{
# Articles from the last week mentioning both terms
query_gdelt(c("measles", "outbreak"), timespan = "1w")

# Daily coverage volume mentioning either phrase, English sources only
query_gdelt(
  c("bird flu", "avian influenza"),
  match = "any",
  mode = "timelinevol",
  start_datetime = as.Date("2025-01-01"),
  end_datetime = as.Date("2025-03-31"),
  source_lang = "english"
)
}
}
\author{
EDG
}
