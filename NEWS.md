# infoveillance NEWS

## Version 0.0.1

- Initial version.
- Added `query_gdelt()` to search the GDELT DOC 2.0 API by keyword and return articles,
  coverage timelines, or tone histograms as a `data.table`.
